/**
 * popup-utils.test-wrapper.cjs
 * Wrapper CommonJS pour les tests Jest
 * Permet d'importer les fonctions ES6 de popup-utils.js dans les tests
 */

// Cette approche ne fonctionne pas car Node.js ne peut pas charger les modules ES6 en mode CommonJS
// Nous allons copier les fonctions dans les tests pour l'apprentissage

module.exports = {
  // Ce fichier est un exemple de ce qu'on pourrait faire
  // mais ne fonctionne pas avec la configuration actuelle
};
